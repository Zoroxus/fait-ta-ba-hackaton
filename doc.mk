## General doc of the API

- GET `http://localhost:3500/mission/` = get all missions 
- GET `http://localhost:3500/mission/:id` = get a mission with chosen id
- POST `http://localhost:3500/mission/` = create a new mission
- DELETE `http://localhost:3500/mission/:id` = delete a misson with chosen id
- PUT `http://localhost:3500/mission/:id` = modify the mission with choosen id

- GET `http://localhost:3500/user/` = get all users
- GET `http://localhost:3500/user/:id` = get the user with choosn id
- POST `http://localhost:3500/user/` = Create a user
- POST `http://localhost:3500/user/login` = Login the user
